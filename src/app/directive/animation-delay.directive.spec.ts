import { AnimationDelayDirective } from './animation-delay.directive';

describe('AnimationDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationDelayDirective();
    expect(directive).toBeTruthy();
  });
});
